<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <!-- Plain net10.0, NOT net10.0-windows. Avalonia is cross-platform, and this is what
         lets the UI be built and headless-tested on macOS — the reason Avalonia was chosen
         over WPF. Windows-only code lives in VoxScribe.Platform.Windows. -->
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>VoxScribe.App</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- The exe's own icon: Explorer, taskbar, Alt-Tab. Same artwork as the macOS bundle,
         converted to a multi-size .ico (16-256 px). -->
    <ApplicationIcon>Assets\app.ico</ApplicationIcon>
    <!-- What Task Manager, file properties and tray hover text call the app. The assembly
         and namespaces stay VoxScribe.* to keep upstream merges clean. -->
    <Product>Vox-Scribe</Product>
    <AssemblyTitle>Vox-Scribe</AssemblyTitle>
    <Version>1.0.0</Version>
    <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
  </PropertyGroup>

  <ItemGroup>
    <AvaloniaResource Include="Assets\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" />
    <PackageReference Include="Avalonia.Desktop" />
    <PackageReference Include="Avalonia.Themes.Fluent" />
    <PackageReference Include="Avalonia.Fonts.Inter" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\VoxScribe.Core\VoxScribe.Core.csproj" />
    <ProjectReference Include="..\VoxScribe.Speech\VoxScribe.Speech.csproj" />
  </ItemGroup>

  <!-- The Windows platform layer targets net10.0-windows and is resolved by reflection at
       run time (see PlatformFactory). ReferenceOutputAssembly=false means it is BUILT and
       COPIED next to the app without being compiled against — which is what lets this
       project stay on plain net10.0 and therefore build and headless-test on macOS.
       Skipped entirely off Windows, where it cannot compile. -->
  <ItemGroup Condition="'$([System.OperatingSystem]::IsWindows())' == 'true'">
    <!-- SkipGetTargetFrameworkProperties is required: without it MSBuild refuses the
         reference outright ("targets net10.0-windows, cannot be referenced by a project
         that targets .NETCoreApp") before ReferenceOutputAssembly is ever considered. -->
    <ProjectReference Include="..\VoxScribe.Platform.Windows\VoxScribe.Platform.Windows.csproj"
                      ReferenceOutputAssembly="false"
                      SkipGetTargetFrameworkProperties="true"
                      UndefineProperties="TargetFramework"
                      Private="false" />
  </ItemGroup>

  <!-- ReferenceOutputAssembly=false suppresses the copy too, so bring the assembly across
       explicitly. PlatformFactory resolves it by name at startup. -->
  <Target Name="PublishWindowsPlatformLayer"
          BeforeTargets="ComputeResolvedFilesToPublishList"
          Condition="'$([System.OperatingSystem]::IsWindows())' == 'true'">
    <!-- RemoveProperties is load-bearing. A publish passes RuntimeIdentifier and
         SelfContained down as global properties, which would send this build to
         bin\$(Configuration)
et10.0-windows\win-x64\ while the copy below reads the
         RID-less path. That mismatch is invisible for as long as an earlier plain build has
         left a stale DLL at the expected path, and becomes "Could not find file" the first
         time someone publishes into a clean tree. -->
    <MSBuild Projects="..\VoxScribe.Platform.Windows\VoxScribe.Platform.Windows.csproj"
             Targets="Build"
             Properties="Configuration=$(Configuration)"
             RemoveProperties="RuntimeIdentifier;SelfContained;PublishSingleFile;PublishDir" />
    <ItemGroup>
      <_PlatformPublish Include="..\VoxScribe.Platform.Windows\bin\$(Configuration)\net10.0-windows\VoxScribe.Platform.Windows.dll" />
      <_PlatformPublish Include="..\VoxScribe.Platform.Windows\bin\$(Configuration)\net10.0-windows\NAudio*.dll" />
      <!-- Loose beside the exe rather than inside the bundle: PublishSingleFile only bundles
           assemblies the compiler knows about, and this one is deliberately invisible to it.
           The default load context probes AppContext.BaseDirectory, and Program.cs installs
           a resolver as a belt-and-braces fallback. -->
      <ResolvedFileToPublish Include="@(_PlatformPublish)">
        <RelativePath>%(Filename)%(Extension)</RelativePath>
        <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      </ResolvedFileToPublish>
    </ItemGroup>
  </Target>

  <Target Name="CopyWindowsPlatformLayer"
          AfterTargets="Build"
          Condition="'$([System.OperatingSystem]::IsWindows())' == 'true'">
    <ItemGroup>
      <PlatformLayer Include="..\VoxScribe.Platform.Windows\bin\$(Configuration)\net10.0-windows\VoxScribe.Platform.Windows.dll" />
      <PlatformLayer Include="..\VoxScribe.Platform.Windows\bin\$(Configuration)\net10.0-windows\NAudio*.dll" />
    </ItemGroup>
    <Copy SourceFiles="@(PlatformLayer)"
          DestinationFolder="$(OutDir)"
          SkipUnchangedFiles="true"
          ContinueOnError="true" />
  </Target>

</Project>
